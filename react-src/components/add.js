import React from 'react';
import { browserHistory } from 'react-router';

const Add = React.createClass({
  render : function(){
    return (
      <div className="add">
        <h1>Add</h1>
      </div>
    )
  }
});

module.exports = Add;